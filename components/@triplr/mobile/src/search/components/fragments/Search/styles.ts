import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },

  search: {
    paddingTop: 41,
    paddingBottom: 18,
    paddingHorizontal: 20,
    backgroundColor: '#fff',
  },
})
